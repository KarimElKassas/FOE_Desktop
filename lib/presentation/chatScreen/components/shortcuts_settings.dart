import 'package:flutter/material.dart';
class ShortCutsSettings extends StatelessWidget {
  const ShortCutsSettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
